{
  "scripts": {
    "build": "elm-make --output=./dist/static/js/elm.js ./src/Main.elm",
    "postinstall": "elm-package install -y",
    "start": "elm-live --path-to-elm-make=./node_modules/.bin/elm-make --host=localhost --port=10033 --dir=./dist --debug --pushstate --output=./dist/elm.js ./src/Main.elm"
  },
  "dependencies": {
    "elm": "^0.18.0",
    "elm-live": "^2.7.3"
  }
}
